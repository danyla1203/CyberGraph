const graphInput = document.querySelector<HTMLInputElement>('#formula') as HTMLInputElement;
const drawGraphBtn = document.getElementById('drawBtn') as HTMLButtonElement;

export { graphInput, drawGraphBtn };
